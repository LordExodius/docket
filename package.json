{
  "name": "docket",
  "version": "1.0.0",
  "description": "markdown notes in new tab",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "browserify": "^17.0.0",
    "tsify": "^5.0.4",
    "tsup": "^8.1.0",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "marked": "^13.0.1"
  }
}
